apiVersion: v2
name: pm
description: Państwa Miasta — gra multiplayer na Kubernetes
type: application
version: 0.1.0
appVersion: "3.0"

dependencies:
  - name: redis
    alias: redisHA
    version: 23.1.1
    repository: oci://registry-1.docker.io/bitnamicharts
    condition: redisHA.enabled
